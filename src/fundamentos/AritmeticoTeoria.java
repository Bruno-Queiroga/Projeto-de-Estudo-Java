package fundamentos;

public class AritmeticoTeoria {
	
	/*Teoria :
	 * - Um operador unário tem um operando. Ex:   a++ este exemplo possuio apenas um operando (a), onde operador aritmetico de soma (++) é de incremento  
	 * 
	 * - Um operador binário tem dois operandos. Ex: 1 (+) 2  sinal de (soma) é o operador  e os números são os operandos, neste caso existe dois operandos sendo
	 *   classificado como operador binário. 
	 *   
	 * - Um operador ternário tem três operandos. Ex:  v (?) v (:) U = Neste exemplo temos um caso de operador ternário, operadorternário tem três operandos
	 * 
	 *   Níveis  de  precedência  mais  utilizado(hierarquia de prioridades) em Java
	 * 
	 * 1. parênteses
	 * 2. operadores unários
	 * 3. ** (exponenciação) -> quando apresentar 
	 * 4. *, /
	 * 5. +, -
	 * 
	 * Exemplos de expressões 
	 * 
	 * Infix    ( O operador aritmetico fica entre os operando )
	 *  A + B
	 *  A + B * C
	 *  3 + 2
	 *  
	 * Prefix  (Na forma prefixada o operador aritmetico estara antes do operando )
	 *  + A B
	 *  + A * B C
	 *  ++ A
	 * 
	 * Posfix ( O operador aritmetico fica depois do operando )
	 *  A B +
	 *  A B C * +
	 *  A ++
	 *  
	 */
	
	

}
